# Optional: canonical origin for Open Graph / link previews (`og:url`, `og:image`, etc.).
# Override on Vercel (or locally) when you use a custom domain.
# VITE_SITE_URL=https://your-domain.com
VITE_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
